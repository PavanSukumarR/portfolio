---
const { projects } = Astro.props;
---
<section class="pt-12 md:pt-16 pb-16 px-6 md:px-20
                bg-gradient-to-b from-neutral-100 to-white dark:from-neutral-900 dark:to-neutral-950
                transition-colors duration-500 ease-in-out" data-aos="fade-up">
  <div class="max-w-6xl mx-auto">
    <div class="prose dark:prose-invert text-left mb-10 font-sans max-w-none">
      <h2 class="font-serif text-3xl md:text-4xl">Selected Projects</h2>
      <p class="text-lg text-neutral-600 dark:text-neutral-400">A focused selection of work exploring performance, usability, and practical problem-solving.</p>
    </div>

    <div class="grid gap-8 md:gap-10 md:grid-cols-2 lg:grid-cols-3 mx-auto">
      {projects.map((project, i) => (
        <div class="bg-neutral-100 dark:bg-neutral-900 rounded-xl p-8 
                    hover:bg-neutral-200 dark:hover:bg-neutral-800 
                    transition-colors duration-200 shadow-md flex flex-col h-full"
             data-aos="zoom-in" data-aos-delay={200 + i * 100}>
          <div class="prose dark:prose-invert font-sans flex-grow">
            <h3 class="font-serif text-2xl mb-3">{project.title}</h3>
            <p class="text-base text-neutral-600 dark:text-neutral-400 mb-6 leading-relaxed">{project.description}</p>
          </div>
          <a href={project.link} target="_blank" class="inline-block text-amber-600 dark:text-amber-500 font-semibold hover:underline mt-auto">View on GitHub →</a>
        </div>
      ))}
    </div>
  </div>
</section>